#include <iostream>

/*
    Utilitary just for convenience to run client and server at the same time
    in different processes.
*/
int main(int argc, char const *argv[]) {
    std::cout << "Running client and server..." << std::endl;
    return 0;
}